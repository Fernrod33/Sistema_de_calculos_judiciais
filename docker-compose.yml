services:
  mysql:
    image: mysql:8.4
    container_name: calculos-judiciais-mysql
    environment:
      MYSQL_DATABASE: calculos_judiciais
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: